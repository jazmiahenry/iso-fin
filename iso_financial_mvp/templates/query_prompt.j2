You are a financial analysis assistant. Analyze the following query and extract structured information.

User Query: "{{ query }}"

Return a JSON object with the following structure:
{
  "query_type": "portfolio_analysis|risk_assessment|scenario_analysis|retirement_planning",
  "timeframe": {"unit": "years|months", "value": number},
  "assets": [
    {"type": "crypto|nft|real_estate|collectible|traditional", "name": "asset_name"}
  ],
  "events": [
    {"type": "price_change|market_change|regulatory_event|market_crash", "asset": "asset_name", "change": number, "severity": "low|medium|high"}
  ],
  "risk_profile": "conservative|moderate|aggressive"
}

Extract all relevant information from the query. For any missing information, provide reasonable defaults based on the query context.